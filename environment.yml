name: student-retention
channels:
  - conda-forge
  - defaults

dependencies:
  - python=3.10
  - pip>=23.0

  # Data handling
  - pandas>=1.5.0
  - numpy>=1.23.0

  # Machine Learning
  - scikit-learn>=1.2.0
  - xgboost>=1.7.0

  # Visualization
  - matplotlib>=3.6.0
  - seaborn>=0.12.0

  # Jupyter
  - jupyter>=1.0.0
  - ipykernel>=6.20.0
  - notebook>=6.5.0

  # Utilities
  - joblib>=1.2.0
  - tqdm>=4.64.0

  # Development
  - pytest>=7.2.0
  - black>=23.0.0
  - flake8>=6.0.0

  # Install from pip
  - pip:
    - ucimlrepo>=0.0.3
    - streamlit>=1.25.0
